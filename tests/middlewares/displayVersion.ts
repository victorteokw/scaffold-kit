import applyMiddleware from '../../src/applyMiddleware';
import Executable from '../../src/Executable';
import displayVersion from '../../src/middlewares/displayVersion';
import nullExecutable from '../../src/nullExecutable';

describe('Displays version', () => {

  it.skip("outputs version to process's stdout", async () => {

  });

  it.skip("prevents execution of next callback", async () => {

  });

  it.skip("appends new line if argument doesn't end with new line", async () => {

  });

  it.skip("keep unmodified if argument ends with new line", async () => {

  });

});
